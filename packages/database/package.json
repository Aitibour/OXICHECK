{
  "name": "@hotelcheckin/database",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma ORM with PostgreSQL schema for HotelCheckIn",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'Database build — not yet scaffolded'",
    "dev": "echo 'Database dev — not yet scaffolded'",
    "lint": "echo 'Database lint — not yet scaffolded'",
    "test": "echo 'Database test — not yet scaffolded'",
    "clean": "rm -rf dist",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@hotelcheckin/shared": "workspace:*"
  }
}
