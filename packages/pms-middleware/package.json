{
  "name": "@hotelcheckin/pms-middleware",
  "version": "0.1.0",
  "private": true,
  "description": "PMS integration middleware — Cloudbeds, Mews, Lightspeed, etc.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hotelcheckin/shared": "workspace:*",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "@types/node": "^20.14.0",
    "typescript": "^6.0.2"
  }
}
